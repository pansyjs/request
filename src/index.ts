export { request, setConfig, updateConfig, } from './request';

export { ErrorShowType } from './config';

export type {
  ResponseData,
  RequestConfig,
  RequestOptions,
  RequestError,
  ErrorHandler,
} from './types';

export type {
  AxiosInstance,
  AxiosRequestConfig,
  AxiosResponse,
} from 'axios';
